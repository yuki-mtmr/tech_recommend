<nav class="navbar navbar-expand-lg navbar-light bg-white mx-auto p-0 my-2" style="width: 80%;">
  <div class="container">
    <a class="navbar-brand " href="/">
      <img src="<%= asset_path "tech_recommend_logo.png" %>" width="30" height="30" class="d-inline-block align-top" alt="ブランド">
        TechRecommend
    </a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">

      <ul class="navbar-nav mr-auto">
        <li class="nav-item">
          <%= link_to "About", about_posts_path, class: "nav-link" %>
        </li>
      </ul>
      <% if user_signed_in? %>
          <ul class="navbar-nav">
            <li class="nav-item">
              <%= link_to "投稿する", new_post_path, class: "nav-link" %>
            </li>
            <div class="dropdown show">
              <li class="nav-item pt-2 mr-2 dropdown-toggle cursor-pointer" role="button" id="dropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                <% if current_user.image.blank? %>
                  <%= image_tag 'default_user.png', size: '30x30' %>
                <% else %>
                  <%= image_tag current_user.image.url, size: '30x30' %>
                <% end %>
                <%= current_user.name %>
              </li>
              <div class="dropdown-menu" aria-labelledby="dropdownMenuLink">
                 <%= link_to "マイページ", user_path(current_user), class: "dropdown-item" %>
                <a class="dropdown-item" href="#">設定</a>
                <%= link_to "ログアウト", destroy_user_session_path, class: "dropdown-item", method: :delete %>
              </div>
            </div>
          </ul>
      <% end %>
    </div>
  </div>
</nav>