  <div class="col-md-4 mb-3">
    <a href="<%= post.url %>" target="_blank" rel="noopener">
      <div class="zoomIn">
        <img src="<%= post.scrape.css('//meta[property="og:image"]/@content').to_s %>" class="card-img-top" alt="...">
      </div>
      <div class="card-body">
        <h5 class="card-title"><%= truncate(post.scrape.title.to_s, length:50)%></h5>
        <p class="card-text"><%= truncate(post.scrape.css('//meta[property="og:image"]/@content').to_s, length:50)%></p>
      </div>
    </a>
      <p class="card-text d-flex justify-content-between">
        <a href="/users/<%= post.user.id %>">
          <%= post.user.name %>
        </a>
        <% if user_signed_in? && current_user.id == post.user_id %>
          <small>
            <%= link_to post_path(post.id), method: :delete do %> 
              <%= icon("fas", "trash-alt")%>
            <% end %>
          </small>
        <% end %>
      </p>
  </div>