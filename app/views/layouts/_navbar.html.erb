<nav class="navbar navbar-expand-lg navbar-light bg-white">
      <div class="container">
        <a class="navbar-brand " href="/">
        <img src="<%= asset_path "tech_recommend_logo.png" %>" width="30" height="30" class="d-inline-block align-top" alt="ブランド">
          TechRecommend
        </a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavDropdown">

          <ul class="navbar-nav mr-auto">　
            <li class="nav-item">
              <a class="nav-link" href="posts/about">About</a>
            </li>
          </ul>
          <% if user_signed_in? %>
              <ul class="navbar-nav">
                <li class="nav-item pt-2  text-muted">
                  <%= "#{current_user.name}さん" %>
                </li>
                <li class="nav-item">
                  <%= link_to "マイページ", "/users/#{current_user.id}", class: "nav-link" %>
                </li>
                <li class="nav-item">
                  <%= link_to "ログアウト", destroy_user_session_path, class: "nav-link", method: :delete %>
                <li class="nav-item">
                  <%= link_to "投稿する", new_post_path, class: "nav-link" %>
                </li>
              </ul>
          <% end %>
        </div>
      </div>
    </nav>